---
layout: default
pagination:
  data: collections.post
  size: 10
  alias: posts
  reverse: true
---

  <section>
    {% for post in posts  %}
      
      <header class="major">
        <img src="{{post.data["header-img"]}}">
        <a href="{{ post.url }}">
          <h2>{{ post.data.title }}</h2>
        </a>
      </header>

      <section>
        <P>{{ post.data.description }}</P>
        <P>{{ post.date | postDate }}</P>
        <ul class="tags">
        {% for tag in post.data.tags %}
        <li>{{tag}}</li>
        {% endfor %}
        </ul>
      </section>

      <hr/>

    {% endfor %}

    <section class="button-container">
        <a href="archives/page2/" class="button"><< Older Posts</a> 
    </section>
  </section>